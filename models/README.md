# Models Storage (`models/`)

This directory holds pre-trained and fine-tuned machine learning model weights required for offline execution.

- `retrieval/`: RemoteCLIP vision-language encoder checkpoints.
- `change_detection/`: Siamese / BIT change detection model checkpoints and classification heads.
